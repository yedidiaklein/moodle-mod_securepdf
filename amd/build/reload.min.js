define(["jquery"],function($){return{init:function(counterParam){let url=new URL(window.location.href);let counter=url.searchParams.get(counterParam);counter=counter?parseInt(counter)+1:1;url.searchParams.set(counterParam,counter);setTimeout(function(){window.location.href=url.toString()},6e4)}}});
